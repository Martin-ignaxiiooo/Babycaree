node_modules/
.env
.env.*
.DS_Store

# Scripts temporales de migración/test con credenciales
# (los scripts reutilizables viven en backend/scripts/, ver README ahí)
run_migration.js
test_db.js
test_smtp.js
migrate*.js

# Archivos de log con posibles credenciales
*.log
backend_error.log

# NOTA: backend/dist/ está actualmente commiteado (ver backend/scripts/README.md
# o preguntarle a Claude por qué antes de cambiar esto — depende de cómo esté
# configurado el Build Command en Render).
# backend/dist/
